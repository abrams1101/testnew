name: Test00
on:
  push:
    branches: 
      - test

jobs: 
  build:
    name: Test00
    runs-on: ubuntu-latest
    steps:
    - name: Step01
      uses: actions/checkout@v2
      
    - name: Step02
      run: ls -la

    - name: Checkout actions
      uses: actions/checkout@v2
      with:
        repository: abrams1101/gh_actions
        ref: test
        path: .github/actions/gh_actions
        
    - name: step03
      run: |
        cd .github/actions/gh_actions && ls -la
